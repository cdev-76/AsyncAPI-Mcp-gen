[project]
name = "asyncapi-mcp-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiokafka>=0.13.0",
    "confluent-kafka[json]>=2.5.0",
    "jsonschema>=4.26.0",
    "python-dotenv>=1.0.0",
    "fastmcp>=2.14.5",
    "mcp>=1.26.0",
    "pydantic>=2.12.5",
    "pyyaml>=6.0.3",
]

[tool.uv.workspace]
members = [
    "api",
]
